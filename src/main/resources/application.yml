server:
    port: 8081
spring:
    datasource:
        password: myroot9
        url: jdbc:mysql://localhost:3306/newdb
        username: root
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
        show-sql: true

        